import { MultiplierDirective } from './multiplier.directive';

describe('MultiplierDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiplierDirective();
    expect(directive).toBeTruthy();
  });
});
